In Swift codebases like Vessel, avoid executing asynchronous I/O operations (such as container start/stop routines) sequentially inside a loop. Instead, wrap them in a `withTaskGroup` or `withThrowingTaskGroup` to process them concurrently, significantly reducing execution latency.
